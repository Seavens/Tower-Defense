// import { type ItemId, ItemKind } from "shared/inventory/types";
// import { itemDefinitions } from "shared/inventory/items";

// export function getTowerCost(id: Option<ItemId>): number {
// 	if (id === undefined) {
// 		return math.huge;
// 	}
// 	const { kind } = itemDefinitions[id];
// 	if (kind.kind !== ItemKind.Tower) {
// 		return math.huge;
// 	}
// 	const { cost } = kind;
// 	return cost;
// }
