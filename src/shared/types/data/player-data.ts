import type { Tower } from "../objects";

export interface PlayerData {
	level: number;
	experience: number;
	gold: number;

	// dailyRewards: {
	// 	lastClaimed: number;
	// 	streak: number;
	// };

	// settings: {
	// 	music: boolean;
	// 	sound: boolean;
	// };
}
